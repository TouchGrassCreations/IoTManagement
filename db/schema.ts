import { sql } from "drizzle-orm";
import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const inventoryParts = sqliteTable("inventory_parts", { id:text("id").primaryKey(), name:text("name").notNull(), normalizedName:text("normalized_name").notNull(), model:text("model"), modelKey:text("model_key").notNull(), category:text("category").notNull(), quantity:integer("quantity").notNull().default(0), description:text("description").notNull().default(""), tags:text("tags").notNull().default("[]"), createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`), updatedAt:text("updated_at").notNull().default(sql`CURRENT_TIMESTAMP`) }, (t)=>[uniqueIndex("idx_inventory_identity").on(t.normalizedName,t.modelKey)]);
export const identificationScans = sqliteTable("identification_scans", { id:text("id").primaryKey(), confirmationTokenHash:text("confirmation_token_hash").notNull().unique(), userId:text("user_id"), provider:text("provider").notNull(), providerModel:text("provider_model").notNull(), acceptedDetectionCount:integer("accepted_detection_count").notNull(), createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
export const identificationEvents = sqliteTable("identification_events", { id:text("id").primaryKey(), scanId:text("scan_id").notNull(), inventoryPartId:text("inventory_part_id").notNull(), source:text("source").notNull(), detectedName:text("detected_name"), detectedModel:text("detected_model"), confirmedName:text("confirmed_name").notNull(), confirmedModel:text("confirmed_model"), quantityAdded:integer("quantity_added").notNull(), confidence:integer("confidence"), visibleMarkings:text("visible_markings").notNull().default("[]"), boundingBox:text("bounding_box"), alternatives:text("alternatives").notNull().default("[]"), wasEdited:integer("was_edited",{mode:"boolean"}).notNull(), createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`) });
